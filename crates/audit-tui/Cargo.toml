[package]
name = "audit-tui"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
audit-core = { path = "../audit-core" }
anyhow = "1"
ratatui = "0.30"
crossterm = "0.29"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }
